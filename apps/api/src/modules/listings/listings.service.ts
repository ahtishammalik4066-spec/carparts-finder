import type { Listing } from "@partfinder/shared";

export async function listListings(query: Record<string, unknown>) {
  // TODO: Implement listings listing logic with filters
  return [];
}

export async function findListing(id: string) {
  // TODO: Implement listing lookup logic
  return { id };
}
